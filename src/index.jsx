import { render } from 'react-dom';

import { App } from './App';

/**
 * render(2 parametros): 
 * 1 - o quer quer que seja exibido em tela;
 * 2 - dentro de qual elemento quer renderizar a informação.
 */
render(<App />, document.getElementById('root'));
